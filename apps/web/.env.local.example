# Copy to .env.local and restart `npm run dev:web` for changes to apply.
NEXT_PUBLIC_API_URL=http://localhost:4000
# Google sign-in button appears automatically once this is set
# (also set GOOGLE_CLIENT_ID in apps/api/.env):
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
