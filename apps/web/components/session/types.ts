/**
 * Public session-page types now live in @/types/session.
 * Re-exported here so existing "@/components/session/types" imports keep working.
 */
export type { Participant } from "@/types/session";
