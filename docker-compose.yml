services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pickleplay
      POSTGRES_PASSWORD: pickleplay
      POSTGRES_DB: pickleplay
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
